package sysml.json_impl;

import org.json.JSONObject;

public class JsonGraphicalEdge extends JsonGraphicalElement
{
   public JsonGraphicalEdge(JsonSystemModel systemModel, JSONObject jObj, JsonGraphicalElement parent)
   {
      super(systemModel, jObj, parent);
   }
   
}
